# Logs
**/logs
**/*.log
**/npm-debug.log*
**/yarn-debug.log*
**/yarn-error.log*
**/pnpm-debug.log*
**/lerna-debug.log*

# Dependencies
**/node_modules
**/vendor

# Exception: Include customized Laravel mail views
!**/resources/views/vendor/

# Environment files
**/.env
**/.env.backup
**/.env.production
**/.env.local
# Exception: the SPA's production config is public - Vite inlines it into the
# bundle - and it must ship so every build resolves the same same-origin API
# URL instead of the developer's localhost .env.
!client/.env.production

# Build and distribution
**/dev-dist
**/dist
**/dist-ssr
# Exception: the built SPA is committed so the single-domain server can serve
# it without a build step. Regenerate with `cd client && pnpm build`.
!client/dist/
**/public/build
**/public/hot
**/public/storage

# Storage
**/storage/*.key
**/storage/pail

# Testing and cache
**/.phpunit.cache
**/.phpunit.result.cache
**/.phpactor.json
# /server/database/seeders/

# Laravel specific
**/Homestead.json
**/Homestead.yaml
**/auth.json

# Editor directories and files
.vscode/*
.agents
.omc
.opencode
!.vscode/extensions.json
.idea
.fleet
.zed
.DS_Store
skills-lock.json
**/*.suo
**/*.ntvs*
**/*.njsproj
**/*.sln
**/*.sw?

# OS generated files
Thumbs.db
Desktop.ini
._*
.Spotlight-V100
.Trashes

# Composer
**/composer.lock

# Coverage reports
**/coverage
**/.coverage

# IDE specific (tambahan)
*.iml
.project
.classpath
.settings/

# Laravel additional
**/storage/debugbar
**/storage/logs/*.log

# Temp files
**/*.tmp
**/*.bak
**/*.swp
**/*~

# Docker
docker-compose.override.yml
.docker/

# Test output
**/test-results
**/.nyc_output

# Graphify
graphify-out/

# Siakang sync (Python bridge)
**/siakang-sync/.venv
**/siakang-sync/uv.lock
**/siakang-sync/.siakang_session_*.json
**/uv.lock
