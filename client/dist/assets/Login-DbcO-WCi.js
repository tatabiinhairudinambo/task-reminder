import{r as e}from"./rolldown-runtime-hePW80VL.js";import{m as t,p as n}from"./vendor-chart-4rmLvWM7.js";import{n as r,o as i,r as a}from"./index-BvU2oWS2.js";import{r as o,t as s}from"./button-BSht_XDO.js";import{r as c,t as l}from"./useSemesterStore-CmsZBQhD.js";import{t as u}from"./PasswordInput-C20nllHx.js";import{t as d}from"./graduation-cap-BxwxdToa.js";import{t as f}from"./authApi-CuxJn5_S.js";import{n as p,r as m}from"./formUtils-sJD8oWYb.js";import{t as h}from"./checkbox-DvJAoT1n.js";import{C as g,S as _,T as v,_ as y,a as b,b as x,c as S,d as C,f as w,g as T,h as E,i as D,l as O,m as k,n as A,o as ee,p as te,r as j,s as M,t as ne,u as re,v as ie,w as N,x as P,y as ae}from"./vendor-three-CGZi2t-s.js";var oe=o(`bell`,[[`path`,{d:`M10.268 21a2 2 0 0 0 3.464 0`,key:`vwvbt9`}],[`path`,{d:`M3.262 15.326A1 1 0 0 0 4 17h16a1 1 0 0 0 .74-1.673C19.41 13.956 18 12.499 18 8A6 6 0 0 0 6 8c0 4.499-1.411 5.956-2.738 7.326`,key:`11g9vi`}]]),F=e(t(),1),I=n(),se=[{icon:c,title:`Jadwal terpusat`,description:`Jadwal kuliah mingguan tersusun otomatis dari mata kuliah yang kamu isi.`},{icon:oe,title:`Pengingat tepat waktu`,description:`Reminder Email atau Telegram sebelum tenggat, plus penanda tugas prioritas.`},{icon:d,title:`Nilai & IPK`,description:`Isi skor sekali, IPK semester dan kumulatif dihitung otomatis.`}],ce=()=>(0,I.jsxs)(`div`,{className:`hidden min-h-0 flex-1 flex-col lg:flex`,children:[(0,I.jsxs)(`div`,{className:`relative min-h-0 flex-1 select-none overflow-hidden`,children:[(0,I.jsx)(`div`,{"aria-hidden":`true`,className:`absolute inset-0`,style:{maskImage:`linear-gradient(to bottom, #000 0%, #000 46%, rgba(0,0,0,0.86) 60%, rgba(0,0,0,0.55) 74%, rgba(0,0,0,0.22) 88%, transparent 100%)`,WebkitMaskImage:`linear-gradient(to bottom, #000 0%, #000 46%, rgba(0,0,0,0.86) 60%, rgba(0,0,0,0.55) 74%, rgba(0,0,0,0.22) 88%, transparent 100%)`},children:(0,I.jsx)(`img`,{src:`/hero-students.webp`,alt:``,className:`absolute inset-0 h-full w-full object-contain object-bottom`,style:{filter:`brightness(1.02) saturate(1.02)`,maskImage:`linear-gradient(to right, transparent 0%, rgba(0,0,0,0.6) 3%, #000 9%, #000 84%, rgba(0,0,0,0.5) 93%, transparent 100%)`,WebkitMaskImage:`linear-gradient(to right, transparent 0%, rgba(0,0,0,0.6) 3%, #000 9%, #000 84%, rgba(0,0,0,0.5) 93%, transparent 100%)`}})}),(0,I.jsxs)(`div`,{className:`absolute inset-x-0 bottom-0 px-10 xl:px-16 2xl:px-24`,children:[(0,I.jsx)(`p`,{className:`mb-3 text-2xl font-semibold text-[#7db4ff] [text-shadow:0_2px_12px_rgba(5,7,13,0.75)] xl:text-3xl [@media(max-height:860px)]:mb-2 [@media(max-height:860px)]:text-xl`,children:`Halo sobat task😊`}),(0,I.jsxs)(`h2`,{className:`max-w-xl text-4xl font-bold leading-tight text-white [text-shadow:0_2px_14px_rgba(5,7,13,0.8)] xl:text-5xl [@media(max-height:860px)]:text-3xl`,children:[`Kelola tugas kuliah,`,(0,I.jsx)(`br`,{}),(0,I.jsx)(`span`,{className:`text-[#7db4ff]`,children:`capai tujuanmu!`})]})]})]}),(0,I.jsxs)(`div`,{className:`shrink-0 px-10 pb-16 [@media(max-height:860px)]:pb-10 xl:px-16 2xl:px-24`,children:[(0,I.jsx)(`p`,{className:`mt-5 max-w-lg text-base leading-relaxed text-white/70 [@media(max-height:860px)]:mt-3 [@media(max-height:860px)]:text-sm`,children:`Task Reminder membantumu mencatat tugas, mengingatkan sebelum tenggat, dan memantau nilai serta IPK semuanya dalam satu tempat.`}),(0,I.jsx)(`div`,{className:`mt-8 grid max-w-2xl gap-6 sm:grid-cols-3 [@media(max-height:860px)]:mt-6`,children:se.map(({icon:e,title:t,description:n})=>(0,I.jsxs)(`div`,{children:[(0,I.jsx)(`div`,{className:`mb-4 flex h-12 w-12 items-center justify-center rounded-xl border border-white/15 bg-white/5 [@media(max-height:860px)]:mb-2 [@media(max-height:860px)]:h-10 [@media(max-height:860px)]:w-10`,children:(0,I.jsx)(e,{className:`h-5 w-5 text-[#9cc4ff]`})}),(0,I.jsx)(`p`,{className:`text-sm font-semibold text-white`,children:t}),(0,I.jsx)(`p`,{className:`mt-1.5 text-xs leading-relaxed text-white/60`,children:n})]},t))})]})]}),L={base:329485,brandBlue:4172277,deepBlue:1920728,violet:7166928,amber:16757847,star:16777215,starBlue:10471679},R={fov:45,near:.5,far:4e3,depth:100},z={nebula:-320,starFar:-460,starMid:-240,starNear:-90,planet:-150,skyline:-100,beams:-94},B={layers:[{count:2400,size:[1,1.7],parallax:.18,twinkle:.3,z:[-430,-270]},{count:1400,size:[1.2,2.2],parallax:.42,twinkle:.45,z:[-250,-140]},{count:380,size:[1.6,2.8],parallax:.85,twinkle:.6,z:[-130,-72]}],calmZone:{x:[.06,.62],y:[.16,.92],keep:.3,dim:.5},pointerTravel:5.5},V={desktop:{fx:.95,fy:1.08,radius:.15,spin:.014},compact:{fx:.97,fy:1.1,radius:.1,spin:.01},segments:96,atmosphereScale:1.06,lightDirection:[-.88,-.1,-.42],surfaceGain:.85,atmosphereIntensity:.4,cityIntensity:1.4,cloudAmount:.26},H={heightFraction:.155,maxHeightFraction:.86,density:.34,minBuildings:26,maxBuildings:64,depthRange:[1.4,4.6],windowScale:[3.4,2.4],windowLit:.07,rimStrength:.85,calmEndX:.64,calmRamp:.12,calmDim:.12},U={count:6,width:[.004,.014],height:[.18,.52],flickerSpeed:[.16,.5],anchorZone:[.56,.99]},W={layers:[{fx:.7,fy:.8,size:[1.15,.72],color:`brandBlue`,opacity:.2,drift:.006,seed:3.1},{fx:.3,fy:.94,size:[.95,.55],color:`violet`,opacity:.14,drift:.009,seed:8.4},{fx:.9,fy:.42,size:[.7,.42],color:`brandBlue`,opacity:.1,drift:.013,seed:15.9}],falloff:2.4},G={strength:.58,radius:.6,threshold:.8};function le(e){let t=new b({canvas:e,antialias:!0,alpha:!1,powerPreference:`high-performance`});t.setClearColor(L.base,1),t.toneMapping=4,t.toneMappingExposure=1.05;let n=new x,r=new T(R.fov,1,R.near,R.far);r.position.set(0,0,0),r.lookAt(0,0,-1);let i={width:1,height:1,aspect:1,pixelRatio:1};function a(e=R.depth){let t=Math.abs(e)||R.depth,n=2*Math.tan(te.degToRad(R.fov)/2)*t;return{width:n*i.aspect,height:n}}function o(e,t,n){let{width:r,height:i}=a(e);return{x:(t-.5)*r,y:(n-.5)*i,width:r,height:i}}function s(e,t){return a(e).width*t}function c(e,t){return a(e).height*t}let l=null,u=null;function d(){l=new D(t),l.addPass(new j(n,r));let e=new N(i.width,i.height);u=new A(e,G.strength,G.radius,G.threshold),l.addPass(u),l.addPass(new ne)}d();function f(e,n){i.width=Math.max(1,Math.floor(e)),i.height=Math.max(1,Math.floor(n)),i.aspect=i.width/i.height,i.pixelRatio=Math.min(window.devicePixelRatio||1,2),t.setPixelRatio(i.pixelRatio),t.setSize(i.width,i.height,!1),r.aspect=i.aspect,r.updateProjectionMatrix(),l.setPixelRatio(i.pixelRatio),l.setSize(i.width,i.height)}function p(){l.render()}function m(){l.dispose(),u?.dispose?.(),t.dispose()}return{renderer:t,scene:n,camera:r,size:i,view:a,at:o,spanW:s,spanH:c,resize:f,render:p,dispose:m}}function K(e=2654435769){let t=e>>>0;return function(){t|=0,t=t+1831565813|0;let e=Math.imul(t^t>>>15,1|t);return e=e+Math.imul(e^e>>>7,61|e)^e,((e^e>>>14)>>>0)/4294967296}}var q=(e,t,n)=>Math.min(n,Math.max(t,e)),J=(e,t,n)=>e+(t-e)*n,Y=(e,t,n,r)=>J(e,t,1-Math.exp(-n*r)),X=(e,[t,n])=>J(t,n,e()),ue=(e,t)=>t[Math.floor(e()*t.length)%t.length],de=`
    uniform float uTime;
    uniform float uSize;
    uniform float uDpr;
    uniform float uTwinkle;
    uniform float uOpacity;

    attribute float aSize;
    attribute float aPhase;
    attribute float aSpeed;
    attribute vec3 aColor;

    varying vec3 vColor;
    varying float vTwinkle;

    void main() {
        vColor = aColor;

        // Slow per-star breathing. The phase offset keeps the field from
        // pulsing in unison, which would read as a flicker of the whole sky.
        float twinkle = 0.72 + 0.28 * sin(uTime * aSpeed + aPhase);
        vTwinkle = mix(1.0, twinkle, uTwinkle);

        vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
        gl_Position = projectionMatrix * mvPosition;

        // Size is deliberately NOT perspective-attenuated. The layers span a
        // huge depth range (z -430 to -72), so inverse-depth falloff would make
        // the far layer sub-pixel and the near layer huge blobs. Each layer's
        // own size range already encodes how near it is, which is easier to
        // tune and stable at any resolution.
        gl_PointSize = uSize * aSize * uDpr;
    }
`,fe=`
    uniform float uOpacity;

    varying vec3 vColor;
    varying float vTwinkle;

    void main() {
        // Round the square point sprite off, with a tight core and a small
        // halo. The core has to stay near-solid: a purely smooth falloff makes
        // a 1-2px star effectively invisible.
        vec2 offset = gl_PointCoord - vec2(0.5);
        float distance = length(offset);
        if (distance > 0.5) discard;

        float core = smoothstep(0.5, 0.16, distance);
        float alpha = core * vTwinkle * uOpacity;

        gl_FragColor = vec4(vColor, alpha);
    }
`;function pe(e,t,n){let{count:r,size:i,z:a}=t,o=new Float32Array(r*3),s=new Float32Array(r*3),c=new Float32Array(r),l=new Float32Array(r),u=new Float32Array(r),d=1.35,f=new O(L.star),p=new O(L.starBlue),m=new O,h=B.calmZone,[g,y]=h.x,[b,x]=h.y,C=new Float32Array(r*3),w=0;for(let e=0;e<r;e+=1){let e=n(),t=n(),r=e>=g&&e<=y&&t>=b&&t<=x,o=1;if(r){if(n()>h.keep)continue;o=h.dim}let d=X(n,a),_=w*3;C[_]=e,C[_+1]=t,C[_+2]=d,m.copy(n()<.22?p:f),m.multiplyScalar(o*X(n,[.65,1])),s[_]=m.r,s[_+1]=m.g,s[_+2]=m.b,c[w]=X(n,i),l[w]=n()*Math.PI*2,u[w]=X(n,[.35,1.15]),w+=1}let T=new S;T.setAttribute(`position`,new M(o.subarray(0,w*3),3)),T.setAttribute(`aColor`,new M(s.subarray(0,w*3),3)),T.setAttribute(`aSize`,new M(c.subarray(0,w),1)),T.setAttribute(`aPhase`,new M(l.subarray(0,w),1)),T.setAttribute(`aSpeed`,new M(u.subarray(0,w),1));function E(){for(let t=0;t<w;t+=1){let n=t*3,r=C[n],i=C[n+1],a=C[n+2],s=e.view(a);o[n]=(r-.5)*s.width*d,o[n+1]=(i-.5)*s.height*d,o[n+2]=a}T.attributes.position.needsUpdate=!0;let t=e.view(z.starMid);T.boundingSphere=new _(new v,Math.hypot(t.width,t.height)*d)}E();let D=new P({uniforms:{uTime:{value:0},uSize:{value:1},uDpr:{value:Math.min(window.devicePixelRatio||1,2)},uTwinkle:{value:t.twinkle},uOpacity:{value:1}},vertexShader:de,fragmentShader:fe,transparent:!0,depthWrite:!1,blending:2}),k=new ie(T,D);return k.frustumCulled=!1,{object:k,parallax:t.parallax,drift:t.drift,baseZ:0,update(e,n,r){D.uniforms.uTime.value=e,k.rotation.z=e*(t.drift??0)*.01,k.position.x=r.x*B.pointerTravel*t.parallax,k.position.y=r.y*B.pointerTravel*t.parallax*.6},layout(){E()},setDpr(e){D.uniforms.uDpr.value=e},dispose(){T.dispose(),D.dispose()}}}function me(e){let t=K(1369948382),n=B.layers.map(n=>{let r=pe(e,n,t);return e.scene.add(r.object),r});return{update(e,t,r){n.forEach(n=>n.update(e,t,r))},layout(){n.forEach(e=>e.layout())},setPixelRatio(e){n.forEach(t=>t.setDpr(e))},dispose(){n.forEach(t=>{e.scene.remove(t.object),t.dispose()})}}}var Z=`
    float hash21(vec2 p) {
        p = fract(p * vec2(123.34, 456.21));
        p += dot(p, p + 45.32);
        return fract(p.x * p.y);
    }

    float valueNoise(vec2 p) {
        vec2 i = floor(p);
        vec2 f = fract(p);
        vec2 u = f * f * (3.0 - 2.0 * f);
        float a = hash21(i);
        float b = hash21(i + vec2(1.0, 0.0));
        float c = hash21(i + vec2(0.0, 1.0));
        float d = hash21(i + vec2(1.0, 1.0));
        return mix(mix(a, b, u.x), mix(c, d, u.x), u.y);
    }

    float fbm(vec2 p) {
        float sum = 0.0;
        float amplitude = 0.5;
        for (int i = 0; i < 5; i++) {
            sum += amplitude * valueNoise(p);
            p = p * 2.03 + vec2(17.1, 9.7);
            amplitude *= 0.5;
        }
        return sum;
    }
`,he=`
    varying vec3 vPosition;
    varying vec3 vNormal;
    varying vec3 vWorldNormal;
    varying vec3 vViewPosition;

    void main() {
        vPosition = position;
        vNormal = normalize(normalMatrix * normal);
        // World-space normal, used for the sun term. The sun has to stay put in
        // world space while the surface spins underneath it — computing the
        // terminator from an object-space normal would drag the day/night line
        // around with the rotation.
        vWorldNormal = normalize(mat3(modelMatrix) * normal);
        vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
        vViewPosition = -mvPosition.xyz;
        gl_Position = projectionMatrix * mvPosition;
    }
`,ge=`
    uniform vec3 uDeepColor;
    uniform vec3 uShallowColor;
    uniform vec3 uLandColor;
    uniform vec3 uIceColor;
    uniform vec3 uCityColor;
    uniform vec3 uLightDirection;
    uniform float uTime;
    uniform float uCityIntensity;
    uniform float uGain;
    uniform float uCloudAmount;

    varying vec3 vPosition;
    varying vec3 vNormal;
    varying vec3 vWorldNormal;
    varying vec3 vViewPosition;

    ${Z}

    void main() {
        // Three different spaces, each for one job:
        //   vNormal      — view space, for the rim (angle to the eye).
        //   vWorldNormal — world space, for the sun (a fixed world direction).
        //   vPosition    — object space, for the surface texture, so the
        //                  continents and city lights spin with the planet.
        vec3 normal = normalize(vNormal);
        vec3 worldNormal = normalize(vWorldNormal);
        vec3 viewDirection = normalize(vViewPosition);

        // Domain-warped fbm gives continents with believable coastlines —
        // plain fbm reads as camouflage.
        vec3 samplePoint = normalize(vPosition);
        vec2 sphereUv = vec2(
            atan(samplePoint.z, samplePoint.x) / 6.2831853,
            asin(clamp(samplePoint.y, -1.0, 1.0)) / 3.1415926
        );

        vec2 warp = vec2(
            fbm(sphereUv * 3.1 + 11.3),
            fbm(sphereUv * 3.1 - 27.9)
        );
        float continents = fbm(sphereUv * 4.2 + warp * 0.65);

        // Bias by latitude so the poles gather more land/ice.
        float latitude = abs(samplePoint.y);
        float elevation = continents - 0.34 + latitude * 0.10;

        float landMask = smoothstep(0.02, 0.14, elevation);
        float iceMask = smoothstep(0.74, 0.94, latitude + continents * 0.10);

        vec3 ocean = mix(uDeepColor, uShallowColor,
            smoothstep(-0.30, 0.05, elevation));
        vec3 land = mix(uLandColor, uLandColor * 0.62,
            smoothstep(0.10, 0.34, elevation));

        vec3 surface = mix(ocean, land, landMask);
        surface = mix(surface, uIceColor, iceMask * 0.72);

        // Drifting cloud deck.
        float clouds = fbm(sphereUv * 6.5 + vec2(uTime * 0.010, uTime * 0.004));
        clouds = smoothstep(0.52, 0.86, clouds);
        surface = mix(surface, vec3(0.86, 0.90, 0.96), clouds * uCloudAmount);

        // Day/night terminator. The sun sits almost on the limb, so the sphere
        // renders as a crescent: mostly dark with one bright edge. A front-lit
        // sphere would be a flat bright disc competing with the login form.
        vec3 lightDirection = normalize(uLightDirection);
        float lambert = dot(worldNormal, lightDirection);
        float daylight = smoothstep(-0.05, 0.55, lambert);

        // City lights: only on the night side, only on land, and clustered
        // rather than dusted evenly.
        float night = 1.0 - daylight;
        float clusters = fbm(sphereUv * 17.0 + 4.7);
        float cityLights = smoothstep(0.58, 0.86, clusters)
            * smoothstep(0.45, 0.95, landMask)
            * night
            * uCityIntensity;

        // Ambient floor keeps the dark side from going pure black, so the
        // sphere still reads as a body and not as a hole in the starfield.
        vec3 color = surface * (0.055 + daylight * 1.0);
        color += uCityColor * cityLights;

        // Rim: a thin bright edge where the sphere turns away from the eye, so
        // the silhouette separates from the black sky.
        float rim = pow(1.0 - max(dot(normal, viewDirection), 0.0), 3.2);
        color += uShallowColor * rim * (0.10 + daylight * 0.5);

        // Final gain so the whole body recedes behind the UI instead of
        // competing with it.
        gl_FragColor = vec4(color * uGain, 1.0);
    }
`,_e=`
    varying vec3 vNormal;
    varying vec3 vWorldNormal;
    varying vec3 vViewPosition;

    void main() {
        vNormal = normalize(normalMatrix * normal);
        vWorldNormal = normalize(mat3(modelMatrix) * normal);
        vec4 mvPosition = modelViewMatrix * vec4(position, 1.0);
        vViewPosition = -mvPosition.xyz;
        gl_Position = projectionMatrix * mvPosition;
    }
`,ve=`
    uniform vec3 uColor;
    uniform vec3 uLightDirection;
    uniform float uIntensity;

    varying vec3 vNormal;
    varying vec3 vWorldNormal;
    varying vec3 vViewPosition;

    void main() {
        vec3 normal = normalize(vNormal);
        vec3 viewDirection = normalize(vViewPosition);

        // Back-face shell: the fresnel peaks exactly where the shell's rim
        // sits over the planet's limb, giving a soft halo with no blur pass.
        float fresnel = pow(1.0 - abs(dot(normal, viewDirection)), 2.6);

        float daylight = smoothstep(-0.35, 0.5, dot(normalize(vWorldNormal), normalize(uLightDirection)));

        float alpha = fresnel * uIntensity * (0.22 + daylight * 0.95);
        gl_FragColor = vec4(uColor, alpha);
    }
`;function ye(e){let t=new C;e.scene.add(t);let n=new g(1,V.segments,V.segments/2),r=new P({uniforms:{uDeepColor:{value:new O(464435)},uShallowColor:{value:new O(L.brandBlue)},uLandColor:{value:new O(860485)},uIceColor:{value:new O(10467028)},uCityColor:{value:new O(L.amber)},uLightDirection:{value:new v(...V.lightDirection).normalize()},uTime:{value:0},uCityIntensity:{value:V.cityIntensity},uGain:{value:V.surfaceGain},uCloudAmount:{value:V.cloudAmount}},vertexShader:he,fragmentShader:ge}),i=new E(n,r),a=new g(1,V.segments/2,V.segments/4),o=new P({uniforms:{uColor:{value:new O(L.brandBlue)},uLightDirection:{value:new v(...V.lightDirection).normalize()},uIntensity:{value:V.atmosphereIntensity}},vertexShader:_e,fragmentShader:ve,transparent:!0,depthWrite:!1,side:1,blending:2}),s=new E(a,o);t.add(i,s);let c={x:0,y:0,radius:1},l=0;function u(t){let n=t?V.compact:V.desktop,{x:r,y:i,height:a}=e.at(z.planet,n.fx,n.fy);c.x=r,c.y=i,c.radius=a*n.radius,l=n.spin}u(!1),t.position.set(c.x,c.y,z.planet),i.scale.setScalar(c.radius),s.scale.setScalar(c.radius*V.atmosphereScale);function d(e,n=!1){u(e),n&&(t.position.set(c.x,c.y,z.planet),i.scale.setScalar(c.radius),s.scale.setScalar(c.radius*V.atmosphereScale))}return{object:t,layout:d,update(e,n){t.position.x=Y(t.position.x,c.x,3.5,n),t.position.y=Y(t.position.y,c.y,3.5,n);let a=Y(i.scale.x,c.radius,3.5,n);i.scale.setScalar(a),s.scale.setScalar(a*V.atmosphereScale),r.uniforms.uTime.value=e,i.rotation.y+=l*n,i.rotation.z=J(i.rotation.z,-.22,.02)},dispose(){e.scene.remove(t),n.dispose(),r.dispose(),a.dispose(),o.dispose()}}}var be=`
    varying vec2 vUv;

    void main() {
        vUv = uv;
        gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);
    }
`,xe=`
    uniform vec3 uColor;
    uniform float uTime;
    uniform float uSeed;
    uniform float uOpacity;
    uniform float uDrift;
    uniform float uFalloff;

    varying vec2 vUv;

    ${Z}

    void main() {
        // Elliptical mask, so the sheet fades to nothing on every side.
        vec2 centred = vUv - vec2(0.5);
        float radius = length(centred * vec2(1.0, 1.35));
        float mask = pow(clamp(1.0 - radius * 2.0, 0.0, 1.0), uFalloff);
        if (mask < 0.004) discard;

        // Two octaves drifting against each other give the cloud structure.
        vec2 drift = vec2(uTime * uDrift, uTime * uDrift * 0.35);
        float cloudA = fbm(vUv * 3.2 + uSeed + drift);
        float cloudB = fbm(vUv * 6.4 - uSeed * 1.7 - drift * 1.4);
        float density = mix(0.5, 1.25, cloudA) * mix(0.6, 1.15, cloudB);

        float alpha = mask * density * uOpacity;
        if (alpha < 0.002) discard;

        gl_FragColor = vec4(uColor, alpha);
    }
`,Se=new y(1,1);function Ce(e){let t=new C;e.scene.add(t);let n=W.layers.map(e=>{let n=new P({uniforms:{uColor:{value:new O(L[e.color])},uTime:{value:0},uSeed:{value:e.seed},uOpacity:{value:e.opacity},uDrift:{value:e.drift},uFalloff:{value:W.falloff}},vertexShader:be,fragmentShader:xe,transparent:!0,depthWrite:!1,blending:2}),r=new E(Se,n);return r.frustumCulled=!1,t.add(r),{mesh:r,material:n,layer:e}});function r(t=!1){n.forEach(({mesh:n,material:r,layer:i})=>{let{x:a,y:o,width:s,height:c}=e.at(i.depth,i.fx,i.fy);n.position.set(a,o,i.depth),n.scale.set(s*i.size[0],c*i.size[1],1),r.uniforms.uOpacity.value=t?i.opacity*.6:i.opacity})}return r(!1),{object:t,layout:r,update(e){n.forEach(({material:t})=>{t.uniforms.uTime.value=e})},dispose(){e.scene.remove(t),n.forEach(({material:e})=>e.dispose())}}}var we=`
    uniform float uCalmEnd;
    uniform float uCalmRamp;

    varying vec3 vLocalPosition;
    varying vec3 vNormal;
    varying float vHeight;
    varying vec2 vFaceUv;
    varying float vRimWidth;
    varying float vCalm;

    void main() {
        vLocalPosition = position;
        vNormal = normalize(normalMatrix * normal);
        // Local box space: x/y/z all run -0.5..0.5, so the top face is the
        // only one that needs to be treated as a roof.
        vHeight = position.y;

        // Recover this instance's world size from its matrix. The box is a
        // shared unit cube, so the window grid drawn from local coordinates
        // would be identical on every building — one 2x2 grid stretched over a
        // skyscraper and a hut alike. Multiplying by the instance scale puts
        // the grid back into world units, so a tall tower genuinely gets more
        // rows of windows than a low one.
        vec3 instanceScale = vec3(
            length(instanceMatrix[0].xyz),
            length(instanceMatrix[1].xyz),
            length(instanceMatrix[2].xyz)
        );

        // The lit roof edge has to be a constant thickness in WORLD units. In
        // local box space it would be 4% of the building, so a landmark tower
        // would get a fat coloured cap while a hut got a hairline. Converting a
        // fixed world height back into local space keeps it even everywhere.
        vRimWidth = clamp(0.10 / max(instanceScale.y, 0.001), 0.002, 0.5);

        // Frame position of this instance, so the fragment stage can dim the
        // lit details where the hero copy sits. Computed from the instance
        // matrix rather than the world matrix, because on an InstancedMesh the
        // model matrix is the parent group's, which is shared by every block.
        mat4 worldMatrix = modelMatrix * instanceMatrix;
        vec4 worldPosition = worldMatrix * vec4(position, 1.0);
        vec4 clipPosition = projectionMatrix * viewMatrix * worldPosition;
        float frameX = clipPosition.x / clipPosition.w * 0.5 + 0.5;
        // 0 = fully dimmed (under the copy), 1 = full brightness (city).
        vCalm = smoothstep(uCalmEnd - uCalmRamp, uCalmEnd, frameX);

        vec3 normal = normalize(vNormal);
        if (abs(normal.x) > 0.5) {
            vFaceUv = vec2(position.z * instanceScale.z, position.y * instanceScale.y);
        } else if (abs(normal.z) > 0.5) {
            vFaceUv = vec2(position.x * instanceScale.x, position.y * instanceScale.y);
        } else {
            vFaceUv = vec2(position.x * instanceScale.x, position.z * instanceScale.z);
        }

        gl_Position = clipPosition;
    }
`,Te=`
    uniform vec3 uBodyColor;
    uniform vec3 uWindowColor;
    uniform vec3 uRimColor;
    uniform float uWindowLit;
    uniform float uRimStrength;
    uniform vec2 uWindowScale;
    uniform float uTime;
    uniform float uSeed;
    uniform float uCalmEnd;
    uniform float uCalmRamp;
    uniform float uCalmDim;

    varying vec3 vLocalPosition;
    varying vec3 vNormal;
    varying float vHeight;
    varying vec2 vFaceUv;
    varying float vRimWidth;
    varying float vCalm;

    ${Z}

    void main() {
        // Fade the windows out toward the top of each block and near the base,
        // so the grid never runs edge to edge like a texture.
        float heightFade = smoothstep(-0.5, -0.42, vLocalPosition.y)
            * (1.0 - smoothstep(0.40, 0.5, vLocalPosition.y));

        vec2 cell = floor(vFaceUv * uWindowScale + uSeed);
        float lit = hash21(cell + uSeed * 7.3);
        float on = step(1.0 - uWindowLit, lit);

        // A slow global shimmer reads as windows being switched on and off
        // without animating every cell individually.
        float shimmer = 0.82 + 0.18 * sin(uTime * 0.35 + hash21(cell) * 6.2831853);

        float isRoof = step(0.5, vHeight);
        float windows = on * heightFade * (1.0 - isRoof);

        // Ease the lit details down under the hero copy. The silhouette body
        // stays fully opaque, so the skyline still spans the frame; only the
        // bright parts retreat.
        float calm = mix(uCalmDim, 1.0, vCalm);

        vec3 color = uBodyColor;
        color += uWindowColor * windows * shimmer * calm;

        // Rim-light along the roof line: a constant-thickness lit edge in world
        // units (see vRimWidth), applied only to the side faces so the roof
        // plane itself does not glow.
        float topEdge = smoothstep(0.5 - vRimWidth, 0.5, vHeight);
        float sideFacing = 1.0 - abs(vNormal.y);
        color += uRimColor * topEdge * sideFacing * uRimStrength * calm;

        gl_FragColor = vec4(color, 1.0);
    }
`;function Ee(e){let t=new C;e.scene.add(t);let n=K(3246022381),r=new O(132106),i=new O(L.amber).lerp(new O(L.brandBlue),.4),a=new O(L.brandBlue),o=new ee(1,1,1),s=new P({uniforms:{uBodyColor:{value:r},uWindowColor:{value:i},uRimColor:{value:a},uWindowLit:{value:H.windowLit},uRimStrength:{value:H.rimStrength},uWindowScale:{value:new N(...H.windowScale)},uTime:{value:0},uSeed:{value:.37},uCalmEnd:{value:H.calmEndX},uCalmRamp:{value:H.calmRamp},uCalmDim:{value:H.calmDim}},vertexShader:we,fragmentShader:Te}),c=null,l=[];function u(r,i){let a=Math.round(Math.min(H.maxBuildings,Math.max(H.minBuildings,r*H.density))),u=e.spanH(z.skyline,H.heightFraction)*(i?.7:1),d=r/a;l=[];for(let e=0;e<a;e+=1){let t=u*(.18+n()**2.1*H.maxHeightFraction),i=X(n,H.depthRange);l.push({x:(e+.5)*d-r/2,y:0,z:X(n,[-i*.35,i*.35]),width:d*X(n,[.72,1.16]),height:t,depth:i,tint:X(n,[.82,1.08])})}c&&(t.remove(c),c.dispose()),c=new w(o,s,l.length),c.instanceMatrix.setUsage(re),c.frustumCulled=!1;let f=new k,p=new v,m=new ae,h=new v;l.forEach((e,t)=>{p.set(e.x,e.y+e.height/2,e.z),h.set(e.width,e.height,e.depth),f.compose(p,m,h),c.setMatrixAt(t,f)}),c.instanceMatrix.needsUpdate=!0,t.add(c)}function d(t){let{width:n}=e.view(z.skyline);u(n,t)}function f(n){let{height:r}=e.view(z.skyline);t.position.y=-r/2+(n?-.5:0),t.position.z=z.skyline}return f(!1),d(!1),{object:t,get buildingAnchors(){return l},layout(e){f(e),d(e)},update(e){s.uniforms.uTime.value=e},dispose(){e.scene.remove(t),c?.dispose(),o.dispose(),s.dispose()}}}var De=`
    varying vec2 vUv;

    void main() {
        vUv = uv;
        gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);
    }
`,Oe=`
    uniform vec3 uBaseColor;
    uniform vec3 uTipColor;
    uniform float uTime;
    uniform float uSeed;
    uniform float uIntensity;
    uniform float uSpeed;

    varying vec2 vUv;

    ${Z}

    void main() {
        float along = vUv.y;

        // Bright at the root, gone by the tip. Squared so the falloff hugs the
        // skyline instead of washing the whole upper frame.
        float lengthFade = pow(1.0 - along, 2.2);

        // A tight bright core inside a softer halo. A single wide falloff reads
        // as a smoke plume; two terms read as a light shaft.
        float across = 1.0 - abs(vUv.x - 0.5) * 2.0;
        float halo = pow(clamp(across, 0.0, 1.0), 1.5);
        float core = pow(clamp(across, 0.0, 1.0), 7.0);

        // Two frequencies of noise, drifting upward, break the shaft into
        // something closer to atmospheric scatter than a painted stripe. Kept
        // shallow (mix floor near 1.0) so the shaft stays a shaft.
        float drift = uTime * uSpeed;
        float noiseA = fbm(vec2(vUv.x * 3.0 + uSeed, along * 4.0 - drift * 0.35));
        float noiseB = valueNoise(vec2(vUv.x * 9.0 - uSeed * 3.0, along * 11.0 - drift));
        float breakUp = mix(0.82, 1.0, noiseA) * mix(0.9, 1.0, noiseB);

        // Very slow flicker, anchored near 1.0 so it never blinks off.
        float flicker = 0.86 + 0.14 * sin(uTime * 0.5 + hash21(vec2(uSeed)) * 6.2831853);

        float profile = halo * 0.55 + core * 0.85;
        float alpha = lengthFade * profile * breakUp * flicker * uIntensity;
        if (alpha < 0.002) discard;

        vec3 color = mix(uBaseColor, uTipColor, smoothstep(0.0, 0.75, along));
        // The core runs hotter and whiter than the halo, the way a bright
        // source blooms out toward white at its centre.
        color = mix(color, vec3(1.0), core * 0.35);
        gl_FragColor = vec4(color, alpha);
    }
`,ke=new y(1,1);function Ae(e,t){let n=new C;e.scene.add(n);let r=K(1856266010),i=new O(L.amber),a=new O(L.brandBlue),o=[];function s(){let{height:t}=e.view(z.beams);n.position.y=-t/2,n.position.z=z.beams}function c(){o.forEach(e=>{n.remove(e.object),e.material.dispose()}),o.length=0}function l(){c();let{width:s,height:l}=e.view(z.beams),u=t.buildingAnchors;if(!u.length)return;let[d,f]=U.anchorZone,p=e.spanH(z.beams,H.heightFraction);for(let e=0;e<U.count;e+=1){let e=u.filter(e=>{let t=(e.x+s/2)/s;return t>=d&&t<=f});if(!e.length)continue;let t=ue(r,e),c=s*X(r,U.width),m=l*X(r,U.height),h=new P({uniforms:{uBaseColor:{value:i},uTipColor:{value:a},uTime:{value:0},uSeed:{value:r()*20},uIntensity:{value:X(r,[.28,.62])},uSpeed:{value:X(r,U.flickerSpeed)}},vertexShader:De,fragmentShader:Oe,transparent:!0,depthWrite:!1,blending:2,side:2}),g=new E(ke,h);g.scale.set(c,m,1),g.position.set(t.x,p*.45+m/2,X(r,[-1.5,1.5])),g.frustumCulled=!1,n.add(g),o.push({object:g,material:h})}}return s(),l(),{object:n,layout(e){if(e){c();return}s(),l()},update(e){o.forEach(t=>{t.material.uniforms.uTime.value=e})},dispose(){e.scene.remove(n),o.forEach(e=>e.material.dispose()),o.length=0}}}function je(e){let t=le(e),n=[],r=Ce(t),i=me(t),a=ye(t),o=Ee(t),s=Ae(t,o);n.push(r,i,a,o,s);let c=window.matchMedia(`(prefers-reduced-motion: reduce)`),l=c.matches,u={x:0,y:0},d={x:0,y:0},f=!1,p={value:0},m=null,h=0,g=!0,_=!1;function v(){return t.size.width<900}function y(){let e=v();r.layout(e),i.layout(),a.layout(e),o.layout(e),s.layout(e)}function b(){let n=e.clientWidth||window.innerWidth,r=e.clientHeight||window.innerHeight;t.resize(n,r);let a=Math.min(window.devicePixelRatio||1,2);i.setPixelRatio(a),y(),l&&x()}function x(){t.render()}function S(e){let n=t.size.width||1,r=t.size.height||1;u.x=q(e.clientX/n*2-1,-1,1),u.y=q(-(e.clientY/r*2-1),-1,1)}function C(e){if(_)return;m=window.requestAnimationFrame(C);let t=e*.001,r=h===0?.016:Math.min(.05,t-h);h=t,g&&(p.value+=r,d.x=Y(d.x,u.x,4.5,r),d.y=Y(d.y,u.y,4.5,r),n.forEach(e=>e.update?.(p.value,r,d)),x())}function w(){m===null&&(h=0,m=window.requestAnimationFrame(C))}function T(){m!==null&&(window.cancelAnimationFrame(m),m=null)}function E(){g=!document.hidden,g&&!l&&(T(),w())}function D(e){l=e.matches,l?(T(),x()):w()}return l||(window.addEventListener(`pointermove`,S,{passive:!0}),f=!0,w()),window.addEventListener(`resize`,b),document.addEventListener(`visibilitychange`,E),c.addEventListener(`change`,D),b(),l&&x(),{resize:b,destroy(){_=!0,T(),window.removeEventListener(`resize`,b),document.removeEventListener(`visibilitychange`,E),c.removeEventListener(`change`,D),f&&window.removeEventListener(`pointermove`,S),n.forEach(e=>e.dispose?.()),t.dispose()},stage:t,depth:z}}var Me=({className:e=``})=>{let t=(0,F.useRef)(null);return(0,F.useEffect)(()=>{let e=t.current;if(!e)return;let n=je(e);return()=>n.destroy()},[]),(0,I.jsx)(`canvas`,{ref:t,"aria-hidden":`true`,className:`pointer-events-none fixed inset-0 -z-10 h-full w-full ${e}`})},Ne=`h-12 border-white/20 bg-white/10 pl-10 text-white placeholder:text-white/50 focus-visible:ring-white/60`,Q=`h-12 border-white/20 bg-white/10 pl-10 text-white placeholder:text-white/50 focus-visible:ring-white/60`,Pe=`border-white/40 data-[state=checked]:border-white data-[state=checked]:bg-white data-[state=checked]:text-[#2563eb]`,$=`relative`,Fe=()=>(0,I.jsxs)(`svg`,{className:`pointer-events-none absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-white/50`,viewBox:`0 0 24 24`,fill:`none`,stroke:`currentColor`,strokeWidth:`2`,strokeLinecap:`round`,strokeLinejoin:`round`,"aria-hidden":`true`,children:[(0,I.jsx)(`rect`,{width:`20`,height:`16`,x:`2`,y:`4`,rx:`2`}),(0,I.jsx)(`path`,{d:`m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7`})]}),Ie=()=>(0,I.jsxs)(`svg`,{className:`pointer-events-none absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-white/50`,viewBox:`0 0 24 24`,fill:`none`,stroke:`currentColor`,strokeWidth:`2`,strokeLinecap:`round`,strokeLinejoin:`round`,"aria-hidden":`true`,children:[(0,I.jsx)(`rect`,{width:`18`,height:`11`,x:`3`,y:`11`,rx:`2`,ry:`2`}),(0,I.jsx)(`path`,{d:`M7 11V7a5 5 0 0 1 10 0v4`})]}),Le=()=>(0,I.jsxs)(`svg`,{className:`mr-2 h-4 w-4`,viewBox:`0 0 24 24`,fill:`none`,stroke:`currentColor`,strokeWidth:`2`,strokeLinecap:`round`,strokeLinejoin:`round`,"aria-hidden":`true`,children:[(0,I.jsx)(`path`,{d:`M5 12h14`}),(0,I.jsx)(`path`,{d:`m12 5 7 7-7 7`})]}),Re=()=>{let[e,t]=(0,F.useState)(``),[n,o]=(0,F.useState)(``),[c,d]=(0,F.useState)(!1),[g,_]=(0,F.useState)({}),[v,y]=(0,F.useState)(!1),b=i();return(0,F.useEffect)(()=>{let e=localStorage.getItem(`token`),t=localStorage.getItem(`isEmailVerified`);e&&t===`true`&&b(`/dashboard`)},[b]),(0,F.useEffect)(()=>{document.title=`Masuk - Task Reminder`},[]),(0,I.jsxs)(`div`,{className:`relative isolate min-h-screen overflow-hidden bg-[#05070d]`,children:[(0,I.jsx)(Me,{}),(0,I.jsxs)(`div`,{className:`relative z-10 flex min-h-screen flex-col lg:flex-row`,children:[(0,I.jsx)(ce,{}),(0,I.jsx)(`div`,{className:`flex flex-1 flex-col items-center justify-center px-4 py-10 lg:px-8`,children:(0,I.jsxs)(`div`,{className:`flex w-full max-w-md flex-col`,children:[(0,I.jsxs)(`div`,{className:`mb-8 flex flex-col items-center`,children:[(0,I.jsx)(`img`,{src:`/logo.webp`,className:`h-32 w-32`,alt:`logo`}),(0,I.jsxs)(`span`,{className:`text-2xl font-bold tracking-tight`,children:[(0,I.jsx)(`span`,{className:`text-white`,children:`Task`}),` `,(0,I.jsx)(`span`,{className:`text-[#7db4ff]`,children:`Reminder`})]})]}),(0,I.jsx)(`h1`,{className:`text-center text-3xl font-bold text-white`,children:`Selamat datang kembali!`}),(0,I.jsx)(`p`,{className:`mb-6 mt-2 text-center text-sm text-white/70`,children:`Masuk untuk melanjutkan ke akun kamu.`}),(0,I.jsxs)(`form`,{onSubmit:async t=>{t.preventDefault();let i=p({email:e,password:n},[{name:`email`,label:`Email`},{name:`password`,label:`Kata Sandi`}]);if(Object.keys(i).length>0){_(i);return}let a={email:e,password:n,remember_me:c};try{y(!0),_({});let e=await f.login(a);r.success(e?.data?.message),localStorage.setItem(`token`,e.data.data.token),localStorage.setItem(`email`,e.data.data.user.email),localStorage.setItem(`name`,e.data.data.user.name),l.getState().setUserName(e.data.data.user.name);let t=(await f.checkEmail()).data?.data?.verified===!0;localStorage.setItem(`isEmailVerified`,t),b(t?`/dashboard`:`/auth/verify-email`)}catch(e){let t=e.response?.data?.errors||{};_(t),r.error(e.response?.data?.message||`Gagal masuk`)}finally{y(!1)}},className:`space-y-4`,children:[(0,I.jsxs)(`div`,{className:$,children:[(0,I.jsx)(Fe,{}),(0,I.jsx)(m,{type:`email`,placeholder:`Masukkan email Anda`,value:e,autoComplete:`username`,required:!0,onChange:e=>t(e.target.value),className:Ne}),g.email?(0,I.jsx)(`p`,{className:`mt-1 text-sm text-red-300`,children:g.email}):null]}),(0,I.jsxs)(`div`,{className:`${$} [&_button]:text-white/60 hover:[&_button]:text-white`,children:[(0,I.jsx)(Ie,{}),(0,I.jsx)(u,{placeholder:`Masukkan kata sandi Anda`,value:n,onChange:o,autoComplete:`current-password`,required:!0,className:Q}),g.password?(0,I.jsx)(`p`,{className:`mt-1 text-sm text-red-300`,children:g.password}):null]}),(0,I.jsxs)(`div`,{className:`flex items-center justify-between`,children:[(0,I.jsxs)(`label`,{className:`flex cursor-pointer items-center gap-2`,children:[(0,I.jsx)(h,{checked:c,onCheckedChange:e=>d(e===!0),className:Pe}),(0,I.jsx)(`span`,{className:`text-sm text-white/80`,children:`Ingat saya`})]}),(0,I.jsx)(a,{to:`/auth/forgot-password`,className:`text-sm text-[#7db4ff] transition-colors hover:text-white hover:underline`,children:`Lupa kata sandi?`})]}),(0,I.jsxs)(s,{type:`submit`,className:`h-12 w-full bg-[#2f6fed] text-white shadow-lg shadow-blue-900/40 hover:bg-[#2a63d4]`,children:[(0,I.jsx)(Le,{}),v?`Memproses...`:`Masuk`]})]}),(0,I.jsxs)(`p`,{className:`mt-6 text-center text-sm text-white/70 lg:text-left`,children:[`Belum punya akun?`,` `,(0,I.jsx)(a,{to:`/auth/register`,className:`font-medium text-[#7db4ff] hover:text-white hover:underline`,children:`Daftar di sini`})]})]})})]})]})};export{Re as default};